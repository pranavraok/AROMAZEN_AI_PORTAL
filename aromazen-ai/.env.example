# Browser requests stay same-origin; Next.js forwards them to FastAPI.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Used by Next.js only. Change this for Docker or production deployment.
BACKEND_API_ORIGIN=http://localhost:8000
